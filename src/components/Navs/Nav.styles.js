const navLinks = {
  color: 'text-gray-700',
  size: 'font-medium',
  space: 'tracking-wide',
  animation: 'transition-colors duration-400',
  pseudoEffects: 'hover:cursor-pointer hover:text-purple-400',
};

export const NavStyles = {
  navLinks: `${navLinks.color} ${navLinks.size} ${navLinks.space} ${navLinks.animation} ${navLinks.pseudoEffects}`
};
